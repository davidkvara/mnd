import Head from "next/head";

export default () => (
  <div style={{ textAlign: `center`, fontFamily: `sans-serif` }}>
    <Head>
      <title>MND now</title>
    </Head>
    <h2>MND now</h2>
  </div>
);
